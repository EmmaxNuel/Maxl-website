# Copy to .env.local for local work. Never commit secrets.
# Production values belong in the Vercel dashboard:
# Project → Settings → Environment Variables.

# Official public domain, no trailing slash. Used for
# metadata, canonical URLs, sitemap and robots.
NEXT_PUBLIC_SITE_URL=https://maxl.com

# --- Future integrations (leave empty until needed) ---
# NEXT_PUBLIC_ANALYTICS_ID=
# CONTACT_API_KEY=
